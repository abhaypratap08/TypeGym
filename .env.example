# Copy to ".env.local" for local dev (never commit secrets).
# From https://dashboard.pusher.com/ → Your Channels app → App Keys

NEXT_PUBLIC_PUSHER_KEY=***REMOVED***
NEXT_PUBLIC_PUSHER_CLUSTER=mt1
PUSHER_APP_ID=***REMOVED***
PUSHER_SECRET=***REMOVED***

# Optional: same random string in both places to require a header on POST /api/pusher
# TYPEGYM_PUSHER_TRIGGER_SECRET=
# NEXT_PUBLIC_TYPEGYM_PUSHER_TRIGGER_SECRET=


